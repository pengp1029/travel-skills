# Requirements

Python 3.10+ is required. The current implementation only uses the Python standard library for helper scripts. External service access requires environment variables:

```bash
AMAP_KEY=
WEATHER_KEY=
WEB_SEARCH_BASE_URL=https://api.duckduckgo.com/
```
